# AutoScale, פורטל לקוחות: מדריך הפעלה

## מה קיים עכשיו

- אפליקציית Next.js + TypeScript עם כל מסכי האב-טיפוס: התחברות, בחירת פרויקט, אשף פרויקט חדש (4 שלבים), ופורטל עם 12 מסכים. עברית RTL, בהיר/כהה/מערכת, רספונסיבי מלא.
- לוח Monday מוכן ומחובר לקוד: **"פורטל לקוחות"** ב-workspace של AutoScalehq.io.
  - לוח ראשי: `18424611751` · לוח תת-פריטים: `18424612238`
  - https://rthq-ops.monday.com/boards/18424611751
- `lib/monday.ts` ממופה לעמודות האמיתיות של הלוח, כולל פונקציות קריאה וכתיבה.

## איך עובדים עם הלוח (הכול במקום אחד)

שורה בלוח = פרויקט של לקוח. כל השאר חי כתת-פריטים של אותה שורה, והצ'אט של הפורטל הוא ה-Updates של השורה. אתה ממשיך לעבוד ב-Monday איך שאתה רגיל, וכשמשהו צריך להגיע ללקוח, מעדכנים את השורה שלו כאן. הפורטל קורא רק מהלוח הזה.

### עמודות שורת הפרויקט

| עמודה | מה כותבים | איפה זה מוצג בפורטל |
|---|---|---|
| לקוח | שם החברה | כותרות, פרופיל |
| אימיילים של חברי צוות | האימיילים של המשתמשים, מופרדים בפסיק | קובע מי רואה את הפרויקט |
| סטטוס | בבנייה / בבדיקה / הושלם... | לוח בקרה + מסך פרויקט |
| % התקדמות | מספר 0 עד 100 | פס ההתקדמות |
| שלב | טקסט כמו `2/3` | "שלב 2 מתוך 3" |
| דדליין / דדליין מעודכן | תאריכים | לוח בקרה |
| הערת עיכוב | טקסט חופשי | ההערה הצהובה בלוח הבקרה |
| תקציב / שולם עד כה | מספרים | סיכום תשלומים, חשבוניות |
| סבבים כלולים / בשימוש / מחיר סבב נוסף | מספרים | מסך הפרויקט |

### תת-פריטים: עמודת "סוג" קובעת לאיזה מסך זה הולך

| סוג | מופיע במסך | עמודות רלוונטיות |
|---|---|---|
| משימה | משימות | סטטוס (אושר / ממתין לאישור / דורש תשלום), סכום |
| חשבונית | חשבוניות ותשלומים | סטטוס (לתשלום / שולם), סכום, תאריך |
| סבב תיקונים | הפרויקט | סטטוס (אושר / ממתין לבדיקה) |
| אבן דרך | לוח בקרה | סטטוס (הושלם / בתהליך / מתוכנן), תאריך |
| מדריך | מדריכים | קישור (וידאו), שיתוף לצוות (צ'קבוקס) |
| גישה | גישות וסיסמאות | סטטוס, פרטים (קישור/משתמש/הערה) |
| קובץ | קבצים ומסמכים | סטטוס (למשל לחתימה), קישור |
| הפניה | הפניות ועמלות | סטטוס, סכום (עמלה) |
| תוספת | תוספות ושדרוגים | סכום, פרטים |

### צ'אט ועדכונים

כותבים Update על שורת הפרויקט ב-Monday, וזה מה שהלקוח יראה בצ'אט של הפורטל. הודעות שהלקוח שולח מהפורטל ייכתבו חזרה כ-Updates על אותה שורה. הודעת דמו ראשונה כבר שם.

## הרצה מקומית

```bash
npm run dev
```

נפתח על http://localhost:3000. בלי מפתחות המסכים מציגים נתוני דמו.

## חיבור נתונים אמיתיים (לפי הסדר)

1. **Monday**: העתק `.env.local.example` ל-`.env.local` והדבק את הטוקן שלך (Monday ← Developers ← My Access Tokens). מזהה הלוח כבר ממולא.
2. **Supabase (התחברות + 2FA)**: אין עדיין פרויקט Supabase ייעודי לפורטל. שלושת הקיימים בחשבון שייכים לדברים אחרים ולא נגענו בהם. צריך ליצור פרויקט חדש בשם AutoScale-Portal ולמלא URL + anon key ב-`.env.local`.
3. **Stripe**: מפתחות מ-dashboard.stripe.com ל-`.env.local`. ההחלטה אם Stripe או ספק ישראלי עדיין פתוחה.
4. **Vercel**: דחיפה ל-GitHub, חיבור ה-repo, והגדרת אותם משתני סביבה בפרויקט ב-Vercel.

## מבנה הקוד

| מיקום | תוכן |
|---|---|
| `app/` | המסכים. `login`, `projects`, `projects/new`, `portal/*` (12 מסכים) |
| `components/` | ThemeSeg, Toggle, ObShell, icons, סרגל צד וצ'אט של הפורטל |
| `lib/monday.ts` | כל הקריאה והכתיבה ל-Monday, ממופה ללוח האמיתי |
| `lib/auth.ts` | Supabase (ימולא כשיהיה פרויקט) |
| `lib/payments.ts` | Stripe (ימולא אחרי ההחלטה על ספק) |
| `app/globals.css` | כל העיצוב מהאב-טיפוס. לא נוגעים, רק מחברים נתונים |
